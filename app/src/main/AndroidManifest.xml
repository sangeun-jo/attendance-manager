<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sej.attend.attandentmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main_icon"
        android:label="출결벌"
        android:roundIcon="@mipmap/ic_main_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="sej.attend.attandentmanager.ModifyAttend"></activity>
        <activity
            android:name="sej.attend.attandentmanager.MainActivity"
            android:label="출결벌"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>